# API Configuration
# When using Vite proxy, API calls go through /api and are proxied to backend
# VITE_API_URL is not needed in development when using proxy
# For production deployment, set VITE_API_URL to your backend server
VITE_API_URL=http://localhost:5000

# Other environment variables can be added here
# VITE_APP_TITLE=Kutubxona
# VITE_APP_VERSION=1.0.0
